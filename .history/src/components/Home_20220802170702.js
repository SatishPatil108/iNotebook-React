import Notes from './Notes';
import Alert from './Alert';

const Home = (props) => {
    return (
        <div>
            <Notes showAlert={showAlert} />
        </div>
    )
}

export default Home

